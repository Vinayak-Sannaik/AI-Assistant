{
  "name": "ai-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "PYTHONPATH=.deps python3.13 -m uvicorn app.main:app --reload --reload-dir app --host 0.0.0.0 --port 8000",
    "start": "PYTHONPATH=.deps python3.13 -m uvicorn app.main:app --host 0.0.0.0 --port 8000"
  }
}
