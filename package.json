{
  "name": "ai-software-engineering-assistant",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace frontend\" \"npm run start:dev --workspace backend\" \"npm run dev --workspace ai-service\"",
    "build": "npm run build --workspace frontend && npm run build --workspace backend",
    "lint": "npm run lint --workspace frontend && npm run lint --workspace backend",
    "typecheck": "npm run typecheck --workspace frontend && npm run typecheck --workspace backend"
  },
  "workspaces": [
    "frontend",
    "backend",
    "ai-service"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "groq-sdk": "^1.2.1"
  }
}
